# Select genome build

Select and load a valid genome build and its relevant Bioc data
packages.

## Usage

``` r
select_genome(genome_build, verbose = TRUE)
```

## Arguments

- genome_build:

  Genome build to use.

- verbose:

  Print messages.

## Value

Named list
